name: CI Build Check

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  test:
    name: Unit tests (JVM)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17 (OpenJ9)
        uses: actions/setup-java@v4
        with:
          distribution: semeru
          java-version: 17
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run JVM unit tests
        run: ./gradlew :flixelgdx-test:test --no-daemon --warning-mode all
        shell: bash

  javadoc:
    name: Javadoc (doclint)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17 (OpenJ9)
        uses: actions/setup-java@v4
        with:
          distribution: semeru
          java-version: 17
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Javadoc all Java modules
        run: ./gradlew javadocAll --no-daemon --warning-mode all
        shell: bash

  build:
    name: Build (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: windows-latest
            platform: windows
          - os: macos-latest
            platform: macos
          - os: ubuntu-latest
            platform: android
          - os: macos-latest
            platform: ios
          - os: ubuntu-latest
            platform: web
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17 (OpenJ9)
        uses: actions/setup-java@v4
        with:
          distribution: semeru
          java-version: 17
          cache: 'gradle'

      - name: Set up Android SDK
        if: matrix.platform == 'android'
        id: setup-android
        uses: android-actions/setup-android@v3
        with:
          # Space-separated list (multiline is passed as one arg and breaks sdkmanager)
          packages: "tools platform-tools platforms;android-36 build-tools;36.0.0"

      - name: Create local.properties (Android)
        if: matrix.platform == 'android'
        run: |
          SDK_DIR="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          echo "sdk.dir=$SDK_DIR" >> local.properties

      - name: Grant execute permission for gradlew
        if: matrix.os != 'windows-latest'
        run: chmod +x gradlew

      - name: Build (LWJGL3)
        if: matrix.platform == 'linux' || matrix.platform == 'windows' || matrix.platform == 'macos'
        run: ./gradlew :flixelgdx-core:assemble :flixelgdx-lwjgl3:assemble --no-daemon
        shell: bash

      - name: Build (Android)
        if: matrix.platform == 'android'
        run: ./gradlew -PincludeAndroid=true :flixelgdx-core:assemble :flixelgdx-android:assembleRelease --no-daemon
        shell: bash

      - name: Build (iOS)
        if: matrix.platform == 'ios'
        run: ./gradlew :flixelgdx-core:assemble :flixelgdx-ios:assemble --no-daemon
        shell: bash

      - name: Build (TeaVM / web)
        if: matrix.platform == 'web'
        run: ./gradlew :flixelgdx-core:assemble :flixelgdx-teavm:assemble --no-daemon
        shell: bash
